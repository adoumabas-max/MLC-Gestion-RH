spring:
  datasource:
    url: jdbc:h2:mem:mlc;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  jwt:
    secret: test-secret-mlc-rh-0123456789-abcdefghijklmnop
    expiration-min: 60
